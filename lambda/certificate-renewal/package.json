{
  "name": "certificate-renewal",
  "version": "0.0.1",
  "description": "Amazon Lambda function to trigger the renewal of a Let's-encrypt certificate",
  "main": "certificate_renewal.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "assemble": "rm certificate-renewal.zip; zip -r certificate-renewal.zip *.js *.json"
  },
  "author": "Karl Guggisberg",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.360.0"
  }
}
